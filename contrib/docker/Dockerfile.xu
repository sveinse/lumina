FROM scratch
MAINTAINER Svein Seldal <sveinse@seldal.com>

ADD rootfs/ /

ENV \
    DEBIAN_FRONTEND=noninteractive \
    USER_UID=${UID} \
    USER_GID=${GID} \
    DOCKER=1

RUN /tmp/init-lub-xu

CMD ["/bin/bash"]
